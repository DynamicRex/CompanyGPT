import React from 'react';

const Login: React.FC = () => { // Changed component name from SignIn to Login
  return (
    <div>
      <h1>Login Page</h1>
      {/* You can build the login form here later */}
    </div>
  );
};

export default Login;
